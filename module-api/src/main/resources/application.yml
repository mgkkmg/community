spring:
  profiles:
    default: local
  config:
    import:
      - classpath:application-core.yml

---
spring:
  config:
    activate:
      on-profile: local

---
spring:
  config:
    activate:
      on-profile: prod